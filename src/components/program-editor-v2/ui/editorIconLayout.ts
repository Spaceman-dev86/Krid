/** Espacement horizontal entre icônes d'action (toolbar, titre + édition). */
export const EDITOR_ICON_GAP = 'gap-2'

/** Rangée poignée DnD + contenu. */
export const EDITOR_GRIP_ROW_GAP = 'gap-2'

/** Poignée DnD — dimensions communes. */
export const EDITOR_GRIP_BUTTON_CLASS =
  'flex h-8 w-5 shrink-0 cursor-grab items-center justify-center text-[var(--brand)] active:cursor-grabbing'

export const EDITOR_GRIP_ICON_SIZE = 12

/** Attribut marqueur pour les poignées drag (exclues du filtre EditorPointerSensor). */
export const EDITOR_DND_HANDLE = 'data-editor-dnd-handle'
